[package]
name        = "valenx-epicyclic"
description = "Planetary / epicyclic gear-train kinematics: tooth-count meshing constraint, the Willis equation, named fixed-member speed ratios, and the tabular (superposition) method. Research/educational grade — textbook closed-form kinematics validated against analytic ground truth, NOT a production-certified gear-design tool (no module/tolerance/safety-factor/fatigue/load-rating analysis)."

version.workspace      = true
edition.workspace      = true
rust-version.workspace = true
authors.workspace      = true
license.workspace      = true
repository.workspace   = true

[dependencies]
thiserror = { workspace = true }

[lints.rust]
unused_imports = "warn"
missing_docs   = "warn"
